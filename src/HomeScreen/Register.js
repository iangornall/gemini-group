import React from 'react';
import './Register.css';
import Button from '../shared/Button';
let Register = (props) => (
  <div className="register">
    <Button text="Register for awesome things" />
  </div>
)
export default Register;