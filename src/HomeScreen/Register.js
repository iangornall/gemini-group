import React from 'react';
import './Register.css';
import Button from '../shared/Button';
let Register = (props) => (
  <div className="register">
    <Button href="/contact" text="Yes! I would like to register for New Property Updates" />
  </div>
)
export default Register;